hazelcast:
    cluster-name: hazelcast-local-cluster
    map:
        products:
            max-idle-seconds: 60
            eviction:
                eviction-policy: LRU
                max-size-policy: PER_NODE
                size: 100
    network:
        join:
            multicast:
                enabled: true